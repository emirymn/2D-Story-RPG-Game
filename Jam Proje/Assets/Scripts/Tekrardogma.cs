using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tekrardogma : MonoBehaviour
{
    public GameObject yenichar;
    public GameObject spawnbir;
    // Start is called before the first frame update
    void Start()
    {
       // scp = GameObject.Find("_Script").GetComponent<OyunKont>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
